<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yc.lining.mapper.ProductMapper" >




  <select id="selectAllProduct"  resultType="Product">
    select pro_name,pro_tagprice,color,pro_number from product where typesid=1 order by pro_number desc 
  </select>
  <select id="selectAllTypes" resultType="Typesinfo">
      select typesid,typename from typesinfo
  </select>
  <select id="selectAllProById"  parameterType="int" resultType="Product">
     select typesid,pro_name,pro_tagprice,color,pro_number from product where typesid=#{typesid} order by pro_number desc 
  </select>
  <select id="selectSprotPro" resultType="Product"> 
    select  pro_name,pro_tagprice,color,pro_number,rownum from (select * from product  order by pro_number desc ) where rownum &lt; 7 
  </select>
</mapper>